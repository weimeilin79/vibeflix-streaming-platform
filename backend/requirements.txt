fastapi>=0.100.0
uvicorn>=0.20.0
python-multipart>=0.0.6
psycopg2-binary>=2.9.0
google-cloud-storage>=2.0.0
# Verifies Firebase ID tokens for admin sign-in (auth.py). Already arrives as a
# transitive dependency, but pinned here because it is used directly.
google-auth>=2.20.0
google-cloud-run>=0.9.0
requests>=2.25.0
# Model Armor screens the free text on a submission (moderation.py): title,
# description, display name, ad copy. Imported lazily, like google-genai in the
# transcoder, so a missing package degrades to "unscreened" rather than a crash.
google-cloud-modelarmor>=0.1.0
